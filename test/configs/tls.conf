
listen = '0.0.0.0:9876'

data_dir = 'tests/data'

tls {
  ca = 'test/certs/ca.pem'
  cert = 'test/certs/server.pem'
  key = 'test/certs/server-key.pem'
}

logging {
  level = 'trace'
  # file = '/tmp/acl-proxy.logs'
}

